from dataclasses import dataclass
from datetime import datetime, timezone


@dataclass
class HealthStatus:
    status: str
    timestamp: datetime
    version: str

    @classmethod
    def healthy(cls, version: str = "1.0.0") -> "HealthStatus":
        return cls(
            status="healthy",
            timestamp=datetime.now(timezone.utc),
            version=version,
        )
